# syntax=codingbooth/boothfile:1
# Configured by: booth config --no-tui --overwrite --cmd bash --select zig/claude-code

env APT_SNAPSHOT=20260906T000000Z

arg CLAUDE_CODE_VERSION=latest
arg ZIG_VERSION=0.15.1

setup claude-code ${CLAUDE_CODE_VERSION}
setup zig --zig-version ${ZIG_VERSION}
setup zig-code-extension
